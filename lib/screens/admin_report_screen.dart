import 'package:flutter/material.dart';

class AdminReportScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Admin Reports"),
      ),
      body: Center(
        child: Text("Here will be the reports for the admin."),
      ),
    );
  }
}
